apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ include "fridgehub.fullname" . }}.ingress-static
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fridgehub.labels" . | nindent 4 }}
    {{- include "fridgehub.labels" . | nindent 4 }}.ingress
spec:
  rules:
  - http:
      paths:
      - path: /auth
        backend:
          serviceName: {{ .Release.Name  }}-keycloak-http
          servicePort: 80
      - path: /
        backend:
          serviceName: {{ include "fridgehub.fullname" . }}-web-ui
          servicePort: 80
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /counterparties/$2
  name: {{ include "fridgehub.fullname" . }}.ingress-api-counterparties
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fridgehub.labels" . | nindent 4 }}
    {{- include "fridgehub.labels" . | nindent 4 }}.ingress
spec:
  rules:
  - http:
      paths:
      - path: /api/v1/counterparty(/|$)(.*)
        backend:
          serviceName: {{ include "fridgehub.fullname" . }}-counterparty-service
          servicePort: 28080
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /instrument/$2
  name: {{ include "fridgehub.fullname" . }}.ingress-api-instruments
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fridgehub.labels" . | nindent 4 }}
    {{- include "fridgehub.labels" . | nindent 4 }}.ingress
spec:
  rules:
  - http:
      paths:
      - path: /api/v1/instruments(/|$)(.*)
        backend:
          serviceName: {{ include "fridgehub.fullname" . }}-instrument-service
          servicePort: 28080
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /valuation/$2
  name: {{ include "fridgehub.fullname" . }}.ingress-api-valuation
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fridgehub.labels" . | nindent 4 }}
    {{- include "fridgehub.labels" . | nindent 4 }}.ingress
spec:
  rules:
  - http:
      paths:
      - path: /api/v1/valuation(/|$)(.*)
        backend:
          serviceName: {{ include "fridgehub.fullname" . }}-valuation-service
          servicePort: 28080
